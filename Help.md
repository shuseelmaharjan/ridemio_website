To run application with docker file you need to run with command

For development (with hot reload):
docker-compose up dev

For production:-
docker-compose up prod

To stop the application you can use command:
docker-compose down

To install new dependencies you can use command:

docker-componse bulid

again run the above command to start the application